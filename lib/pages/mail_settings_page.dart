import 'package:flutter/material.dart';

class MailSettingsPage extends StatelessWidget {
  const MailSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('邮箱设置页面创建成功'),
    );
  }
}